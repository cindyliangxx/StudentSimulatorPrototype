# 卡牌快速配表工具

双击 `启动卡牌配表工具.bat` 打开工具。

使用流程：

1. 选择剧情 Excel，例如 `剧情表格 (1).xlsx`。
2. 点击 `加载表格`。
3. 检查字段对应是否正确。
4. 逐张卡配置左滑、右滑的四项数值。
5. 点击 `导出 Excel`。
6. 把导出的 Excel 交给 Unity 里的 `Tools > Student Simulator > Card Excel Importer`。

导出的字段和当前 Unity 导入器保持一致。
